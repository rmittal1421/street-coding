#include <iostream>
#include <vector>

using namespace std;

int calculateMinimumHP(vector<vector<int>>& dungeon) {
	std::vector<std::vector<int> v;> life;

	int rows = dungeon[0].size();
	int cols = dungeon.size();

	cout << rows << " " << cols << endl;
}

int main () {
	vector< vector<int> > matrix(m,vector<int>(n));

for(i=0;i<m;i++)
{
   for(j=0;j<n;j++)
   {
      cin>>matrix[i][j];
   }
   cout<<endl;
}
	
	return 0;
}